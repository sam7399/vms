{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npx prisma generate --schema=packages/database/prisma/schema.prisma && npm --workspace=@vms/shared run build && npm --workspace=@vms/api run build"
  },
  "deploy": {
    "startCommand": "npm --workspace=@vms/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
